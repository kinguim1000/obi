
var scanf_stub = require('scanf-printf-obi').scanf,
    printf_stub = require('scanf-printf-obi').sprintf,
    fgets_stub = require('scanf-printf-obi').fgets

function scanf(format) {
    var res = require('scanf-printf-obi').scanf.apply(this, Array.prototype.slice.call(arguments, 0));
    eval(res);
}

function printf(args){
    var res = require('scanf-printf-obi').sprintf.apply(this, Array.prototype.slice.call(arguments, 0));
    process.stdout.write(res);
}

function fgets(args){
    var res = fgets_stub = require('scanf-printf-obi').fgets.apply(this, Array.prototype.slice.call(arguments, 0));
    eval(res);
}

module.exports = {scanf,printf,fgets}